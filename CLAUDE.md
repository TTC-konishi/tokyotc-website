# Claude Code Rules

このプロジェクトで作業する前に、必ず同じディレクトリの `AGENTS.md` を読んでください。

特に重要:

- 本番公開用の正本は `/Users/hkonishi/Projects/tokyotc-website` です。
- 体験メニューは `public/experience/` 配下にあります。
- `/Users/hkonishi/Documents/New project/ttc-retro-ab` は参照元として使うことはありますが、push 先ではありません。
- 画像はまず対象ページ配下の `assets/` と `public/experience/assets/` を確認します。
- Codexなど別エージェントが同じページを作業中の場合、同時編集せずレビュー・相談に留めます。

